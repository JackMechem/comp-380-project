public class ApiController {

    // status codes
    // error handlers that return status codes if can't reach db or improper req or auth or if all went well - 300 maybe add redirects or 404, try-catch 500 if db fails or backend fail
    // needs to have a few paths - database, server, front website
    // GET, POST, etc methods, interface in front to interact so admin adds cars - see carcontroller class functs - getallc, createc, getc, updatec, deletec - need to use car objects - some funct need an input passed throun ctx var from javalin - (json style req)
    // customer reviews done through here




}